{
  "name": "@emergencyai/sdk",
  "version": "0.1.0",
  "description": "Typed API client SDK for EmergencyAI (TS/JS consumers, e.g. the admin panel)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
